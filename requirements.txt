# MemOS-Lite requirements
# Local macOS-friendly setup. Default LLM/embedding backend: Ollama server.

# --- Storage ---
chromadb>=0.5.0
# SQLite is provided by Python's standard library.

# --- Demo UI ---
streamlit>=1.30.0
pandas>=2.2.0

# --- Document ingestion ---
python-docx>=1.1.0
openpyxl>=3.1.0

# --- LLM / embedding backend ---
# The source code communicates with Ollama through HTTP using Python's standard
# library. The Ollama desktop/server application must still be installed and run
# separately. These packages are kept for compatibility and local utilities.
ollama>=0.3.0
requests>=2.31.0

# --- General utilities ---
python-dotenv>=1.0.0
tqdm>=4.66.0
pydantic>=2.7.0

# Optional text repair. ingest.py works without it and falls back safely.
# ftfy>=6.2.0

# --- Evaluation / development ---
pytest>=8.0.0

# --- Optional llama.cpp Python binding ---
# The current LlamaCppClient talks to a running llama.cpp HTTP server, so this is
# not required unless direct in-process llama.cpp integration is added later.
# llama-cpp-python>=0.2.90
